.chat-area__chat__list
  %span.chat-area__chat__list__name
    = message.user.name
  %span.chat-area__chat__list__date
    = message.created_at.strftime("%Y/%m/%d %H:%M:%S")
  %p.chat-area__chat__list__message
    = message.comment
  = image_tag message.image.url if message.image.present?